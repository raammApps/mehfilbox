# Evidence for the 15 September findings

Frozen inputs, not living documents. The seven documents in the parent directory cite these by
name (`map-client.md §7 item 3`, `verify-scalability.json`, `cost.json`). They were produced on
15–16 September 2026 against `main` at `96fb305`, and are kept so a claim can still be checked in
six months. Do not update them; write a new dated set.

| Folder | What | Produced by |
|---|---|---|
| `maps/` | Nine subsystem maps: studio, platform, client, guest, auth-security, commerce, notifications, media-infra, themes-customizer | One reader per subsystem, code first, docs second |
| `reviews/` | Four review lenses (`assess-*.json`) and the adversarial verdict on every finding (`verify-*.json`) | A reviewer, then a second agent told to refute each finding from the code |
| `panels/` | Three port positions and two judges; two transcoding positions, the cost model and one judge; two go-to-market positions (the third and its judges did not return) | Argued positions, scored |
| `research/` | Six Startup India topics with source URLs, and the fact-check of every numeric or dated claim against primary sources | Web research on official portals where they answered |
